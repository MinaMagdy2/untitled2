name: untitled2
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^2.0.9
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1 # إضافة الحزمة الخاصة بالأيقونات
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # إضافة إعدادات الأيقونة
  flutter_icons:
    android: true
    ios: true
    image_path: "assets/icon/app_icon.png"  # المسار إلى صورة الأيقونة الخاصة بك
